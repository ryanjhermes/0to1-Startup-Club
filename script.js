// This JavaScript file is kept minimal as the site is primarily static
// Add any interactive features here if needed in the future

document.addEventListener('DOMContentLoaded', function() {
    console.log('0to1 website initialized');
    
    // Add smooth scrolling for anchor links if needed in the future
    // document.querySelectorAll('a[href^="#"]').forEach(anchor => {
    //   anchor.addEventListener('click', function (e) {
    //     e.preventDefault();
    //     document.querySelector(this.getAttribute('href')).scrollIntoView({
    //       behavior: 'smooth'
    //     });
    //   });
    // });
  });